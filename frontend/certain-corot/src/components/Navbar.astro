---
// src/components/Navbar.astro
const currentPath = Astro.url.pathname;
---
<nav class="w-full bg-white border-b border-gray-200 py-4 px-6 flex justify-between items-center mb-8 shadow-sm">
    <div class="flex gap-6">
        <a href="/" class={`font-bold transition-colors ${currentPath === '/' ? 'text-blue-600' : 'text-gray-500 hover:text-black'}`}>
            🏠 Tasks
        </a>
        <a href="/blog" class={`font-bold transition-colors ${currentPath === '/blog' ? 'text-blue-600' : 'text-gray-500 hover:text-black'}`}>
            📄 Blog Archive
        </a>
    </div>
    <div class="hidden md:block text-[10px] text-gray-400 font-mono italic">
        Payload + Astro Engine
    </div>
</nav>